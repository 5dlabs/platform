# Simple requirements example - just mount entire secrets
# This is the easiest approach when secret keys match desired env var names

secrets:
  # Mount all keys from these secrets as environment variables
  - name: openai-credentials
  - name: redis-credentials
  - name: slack-webhook

environment:
  NODE_ENV: "development"
  DEBUG: "true"

# Environment-only requirements
# No secrets needed for this task, just configuration

environment:
  # Build configuration
  BUILD_TARGET: "production"
  MINIFY_CODE: "true"
  SOURCE_MAPS: "false"
  
  # Performance settings
  MAX_WORKERS: "4"
  MEMORY_LIMIT: "2048"
  TIMEOUT_SECONDS: "30"
